# Build / Buy Register — Historical Input

Status: **superseded for new design work**.

The current sourcing authority is:

`artifacts/business/decisions/platform-sourcing.md`

The new decision keeps the original principle—LenBands owns differentiated learning/evaluation semantics and buys commodity infrastructure—but consolidates the managed platform to reduce provider, IAM, runtime and incident noise.

Compatibility policy marker: LenBands **must not build** commodity platform capability when the managed sourcing authority can satisfy the governed contract.

## Procurement gate

The active Procurement gate is defined in `artifacts/business/decisions/platform-sourcing.md`. This historical file does not select or activate a provider.

This file remains as historical traceability for the earlier provider-sprawl baseline. It must not be used to select a new provider or production topology.
